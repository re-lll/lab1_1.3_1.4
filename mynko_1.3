a)
k += 1		4	
i = n		2
while i > 0:	3 * (n + 1)
	i -= 1	4 * n

sum: 7n + 9


b)
i = n		2
while i > 1:	3 * (m + 1)
	k += 1	4 * m
	i //= 2	4 * m

n = 2^m -> m = log2(n)

sum: 11m + 5 = 11 log2(n) + 5


c) 
i = 0			2
while i < n:		3 * (n/2 + 1)
	j = 0		2 * n/2
	while j < n:	3 * n/2 * (n/2 + 1)
		k += 1	4 * n/2 * n/2
		j += 2	4 * n/2 * n/2
	i += 2		4 * n/2

sum: 11/4n^2 + 6n + 5


d)
i = 0				2
while i < n:			3 * (n+1)
	j = 0			2 * n
	while j < i * i:	5 * (1^2 + 2^2 + ... + (n - 1)^2)
		k += 1		4 * (1^2 + 2^2 + ... + (n - 1)^2)
		j += 1		4 * (1^2 + 2^2 + ... + (n - 1)^2)
	i += 1			4 * n

1^2 + 2^2 + ... + (n - 1)^2 = (n(n - 1)(2n - 1))/6

sum: 26/6 n^3 - 39/6 n^2 + 67/6 n + 5


e) 
i = 1			2
while i < n:		3 * (m + 1)
	j = 1		2 * m
	while j < n:	3 * m * (m + 1)
		k += 1	4 * m * m
		j *= 2	4 * m * m
	i *= 2		4 * m

n = 2^m -> m = log2(n)

11m^2 + 12m + 5 = 11(log2(n))^2 + 12log2(n) + 5


f)
i = 1			2
while i < n:		3 * (m + 1)
	j = 1		2 * m
	while j < n:	3 * m * (log2(n/i) + 1)
		k += 1	4 * m * (log2(n/i))
		j *= 2	4 * m * (log2(n/i))
	i *= 2		4 * m

n = 2^m -> m = log2(n)

sum: 11m*log2(n/i) 12m + 5 = 11log2(n)log2(n/i) + 12log2(n) + 5
